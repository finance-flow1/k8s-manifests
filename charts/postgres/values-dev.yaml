storage:
  storageClassName: standard   # Use local-path or standard for dev (e.g. kind/minikube)
  size: 5Gi
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
