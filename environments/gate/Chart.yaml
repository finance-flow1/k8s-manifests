apiVersion: v2
name: finance-gate
description: FinanceFlow – Gateway namespace chart. Manages the Envoy Gateway resource and HTTPRoutes routing traffic to finance-dev and finance-prod.
type: application
version: 0.1.0
appVersion: "1.0.0"
