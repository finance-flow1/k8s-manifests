---
# Allows the Gateway in 'gate' namespace to reference services in 'finance-prod'
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-gate-to-finance-prod
  namespace: finance-prod
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: gate
  to:
    - group: ""
      kind: Service
